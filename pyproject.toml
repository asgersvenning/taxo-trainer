[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "taxo-trainer"
version = "0.1.0"
description = "Modernized species & higher-rank taxonomy training engine powered by GBIF DarwinCore datasets."
readme = "README.md"
authors = [
    { name = "Taxo-Trainer Development Team" }
]
requires-python = ">=3.10"
dependencies = [
    "nicegui>=2.0.0",
    "numpy>=1.24.0",
    "httpx>=0.27.0",
]

[project.scripts]
taxo-trainer = "src.app:main"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.3.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.uv]
package = true
